#include <avr/io.h>

//#define HPORTB(X) PORTB = PORTB | 1 << X // usage HPORTB(1) will set bit 1 to high. bit is the IO pin that you want to set high or low
//#define LPORTB(X) PORTB = PORTB & ~(1 <<  X)

